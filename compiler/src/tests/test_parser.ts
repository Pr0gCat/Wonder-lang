import Parser from "../parser";
import {describe, it} from "mocha";

describe('Parser', () => {
    
    it('No error emitted with empty text stream', () => {

    });

    it('No error emitted with empty file', () => {

    });

    describe('Comment', () => {
        it('');
    });

});